<UserControl
  x:Class="GMapsSync.Src.Presentation.View.PropertyPageView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:ui="clr-namespace:GMapsSync.Src.Presentation.ViewModel"
  xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
  mc:Ignorable="d"
  d:DesignHeight="600"
  d:DesignWidth="600"
  d:DataContext="{Binding Path=ui.PropertyPageViewModel}"
>
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid Margin="10,10,10,10" Grid.IsSharedSizeScope="True">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="5" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>

    <TextBlock
      Text="Preferencias de Driver y Navegador"
      Style="{DynamicResource Esri_TextBlockH3}"
      Margin="0,0,0,10"
      Grid.Row="0"
      Grid.Column="0"
      Grid.ColumnSpan="3"
    />

    <TextBlock
      x:Name="txtBrowserLabel"
      Text="Navegador:"
      Style="{DynamicResource Esri_TextBlockRegular}"
      Margin="0,0,0,5"
      Grid.Row="1"
      Grid.Column="0"
      Grid.ColumnSpan="3"
    />

    <ComboBox
      x:Name="cmbBrowser"
      Margin="0,0,0,10"
      Grid.Row="2"
      Grid.Column="0"
      Grid.ColumnSpan="3"
      ItemsSource="{Binding Browsers}"
      SelectedItem="{Binding SelectedBrowser, Mode=TwoWay}"
    />

    <TextBlock
      x:Name="txtDriverLabel"
      Text="Seleccionar driver:"
      Style="{DynamicResource Esri_TextBlockRegular}"
      Margin="0,0,0,5"
      Grid.Row="3"
      Grid.Column="0"
      Grid.ColumnSpan="3"
    />

    <TextBox
      x:Name="txtDriverPath"
      Margin="0"
      Height="25"
      Grid.Row="4"
      Grid.Column="0"
      IsReadOnly="True"
      Text="{Binding DriverPath, Mode=TwoWay}" />

    <Button
      x:Name="btnBrowseDriver"
      Content="..."
      Margin="0"
      Style="{DynamicResource Esri_Button}"
      Grid.Row="4"
      Grid.Column="2"
      Command="{Binding BrowseDriverCommand}"
    />
  </Grid>
</UserControl>